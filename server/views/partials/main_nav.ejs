  <!-- Main Navigation -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">

      <!-- Brand Content -->
      <a href="/">
        <img src="/Assets/images/hexacoderslogo.png" alt="Logo" width="" height="70">
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ms-auto mb-2 mb-lg-0">

          <li class="nav-item">
            <% if (title == "Home") { %> 
            <a class="nav-link active" aria-current="page" href="/home"><i class="fas fa-home"></i> Home</a>
            <% } else { %>
              <a class="nav-link" href="/home"><i class="fas fa-home"></i> Home</a>
            <% } %>  
          </li>

          <li class="nav-item">
            <% if (title == "Create Survey") { %> 
              <a class="nav-link active" aria-current="page" href="/surveys/createSurvey"><i class="fas fa-edit"></i> Create Survey</a>
              <% } else { %>
                <a class="nav-link" href="/surveys/createSurvey"><i class="fas fa-edit"></i> Create Survey</a>
              <% } %>  
          </li>
          
          <li class="nav-item">
            <% if (title == "My Surveys") { %> 
              <a class="nav-link active" aria-current="page" href="/surveys"><i class="fas fa-poll"></i> My Surveys</a>
              <% } else { %>
                <a class="nav-link" href="/surveys"><i class="fas fa-poll"></i> My Surveys</a>
              <% } %>
          </li>
          
          <% if(typeof displayName == "string" && displayName) { %>
            <li class="navbar-text">
              <i class="fas fa-lg fa-user"></i> Welcome, <%= displayName %>
            </li>
            <li class="nav-item">
              <a href="/logout" class="nav-link"
                ><i class="fas fa-lg fa-sign-out-alt"></i> Logout</a
              >
            </li>
            <% } else { %>
            <li clas="nav-item">
              <a href="/login" class="nav-link"
                ><i class="fas fa-lg fa-sign-in-alt"></i> Login</a
              >
            </li>
            <% } %>
        </ul>
      </div>
    </div>
  </nav>